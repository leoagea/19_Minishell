#include "../../inc/minishell.h"

void	del_node(t_list *env)
{
	
}

t_list	*min_node(t_list *env)
{

}

t_list	*init_export(t_list	*env)   // INITIALISE LISTE EXPORT
{
	t_list	*export;
	t_list	*new_node;
	char	*temp;
	int		i;

	while (env != NULL)
	{
		temp = env->content;
	}
}